# See https://github.com/Tecnativa/doodba#optodoocustomsrcreposyaml
odoo:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/odoo/odoo.git
  target: origin 12.0
  merges:
    - origin 12.0

apps2grow/apps:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/apps2grow/apps.git
  target: origin 12.0
  merges:
    - origin 12.0

apps2grow/survey:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/apps2grow/survey.git
  target: origin 12.0
  merges:
    - origin 12.0

OCA/account-analytic:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/account-analytic.git
  target: origin 12.0
  merges:
    - origin 12.0

OCA/account-financial-reporting:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/account-financial-reporting.git
  target: origin 12.0
  merges:
    - origin 12.0

OCA/account-financial-tools:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/account-financial-tools.git
  target: origin 12.0
  merges:
    - origin 12.0

OCA/reporting-engine:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/reporting-engine.git
  target: origin 12.0
  merges:
    - origin 12.0

OCA/server-backend:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/server-backend.git
  target: origin 12.0
  merges:
    - origin 12.0

OCA/server-tools:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/server-tools.git
  target: origin 12.0
  merges:
    - origin 12.0

OCA/server-ux:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/server-ux.git
  target: origin 12.0
  merges:
    - origin 12.0

OCA/web:
  defaults:
    depth: $DEPTH_MERGE
  remotes:
    origin: https://github.com/OCA/web.git
  target: origin 12.0
  merges:
    - origin 12.0
